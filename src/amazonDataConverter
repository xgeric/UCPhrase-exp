import json
import utils

inputFile = "/shared/data2/ppillai3/test/UCPhrase-exp/data/kpWater/water.jsonl"
outputFile = "/shared/data2/ppillai3/test/UCPhrase-exp/data/kpWater/standard/kpWater.trainTEST.jsonl"

data = utils.JsonLine.load(inputFile)

# with open(file, 'r') as json_file:
#     json_list = list(json_file)
# for json_str in json_list:
#     result = json.loads(json_str)
#     data.append(result)

print(data[0]['asin'])

eval_result = {
            'infos': data[0]['asin'],
            'test' : 'test'
        }

for product in data:
    utils.JsonLine.dump(eval_result, outputFile)